name: Deploy to AWS ECS

on:
  push:
    branches:
      - main

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
  ECS_CLUSTER: ${{ secrets.ECS_CLUSTER }}
  ECS_SERVICE: ${{ secrets.ECS_SERVICE }}
  IMAGE_TAG: latest
  WEBSITE_URL: http://ALB-web-212639990.ap-southeast-1.elb.amazonaws.com

jobs:
  deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest

    steps:
      # ===== TELEGRAM START =====
      - name: Notify Deploy Started
        continue-on-error: true
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d "text=🚀 DEPLOY STARTED%0A%0AWorkflow: #${{ github.run_number }}%0ARepository: ${{ github.repository }}%0ABranch: ${{ github.ref_name }}%0ACommit: ${{ github.event.head_commit.message }}%0AActor: ${{ github.actor }}"

      # ===== CHECKOUT =====
      - name: Checkout source code
        uses: actions/checkout@v4

      # ===== SETUP NODE =====
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # ===== INSTALL HTML VALIDATOR =====
      - name: Install HTML Validator
        run: |
          npm install -g htmlhint

      # ===== HTML CHECK =====
      - name: Set Current Step
        run: echo "CURRENT_STEP=HTML Validation" >> $GITHUB_ENV

      - name: Validate HTML
        run: |
          htmlhint index.html

      # ===== JS CHECK =====
      - name: Set Current Step
        run: echo "CURRENT_STEP=JavaScript Validation" >> $GITHUB_ENV

      - name: Validate JavaScript
        run: |
          node --check script.js

      # ===== AWS LOGIN =====
      - name: Set Current Step
        run: echo "CURRENT_STEP=AWS Credentials" >> $GITHUB_ENV

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # ===== LOGIN ECR =====
      - name: Set Current Step
        run: echo "CURRENT_STEP=ECR Login" >> $GITHUB_ENV

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # ===== BUILD IMAGE =====
      - name: Set Current Step
        run: echo "CURRENT_STEP=Docker Build" >> $GITHUB_ENV

      - name: Build Docker Image
        run: |
          docker build -t $ECR_REPOSITORY:$IMAGE_TAG .

      # ===== TAG IMAGE =====
      - name: Set Current Step
        run: echo "CURRENT_STEP=Docker Tagging" >> $GITHUB_ENV

      - name: Tag Docker Image
        run: |
          docker tag $ECR_REPOSITORY:$IMAGE_TAG \
          ${{ steps.login-ecr.outputs.registry }}/$ECR_REPOSITORY:$IMAGE_TAG

      # ===== PUSH IMAGE =====
      - name: Set Current Step
        run: echo "CURRENT_STEP=Push To ECR" >> $GITHUB_ENV

      - name: Push Docker Image to ECR
        run: |
          docker push \
          ${{ steps.login-ecr.outputs.registry }}/$ECR_REPOSITORY:$IMAGE_TAG

      # ===== DEPLOY ECS =====
      - name: Set Current Step
        run: echo "CURRENT_STEP=Deploy ECS Service" >> $GITHUB_ENV

      - name: Deploy to ECS Service
        run: |
          aws ecs update-service \
            --cluster $ECS_CLUSTER \
            --service $ECS_SERVICE \
            --force-new-deployment

      # ===== WAIT ECS =====
      - name: Set Current Step
        run: echo "CURRENT_STEP=Wait ECS Stable" >> $GITHUB_ENV

      - name: Wait for ECS Stable
        run: |
          aws ecs wait services-stable \
            --cluster $ECS_CLUSTER \
            --services $ECS_SERVICE

      # ===== WEBSITE HEALTH CHECK =====
      - name: Set Current Step
        run: echo "CURRENT_STEP=Website Health Check" >> $GITHUB_ENV

      - name: Check Website Health
        run: |
          STATUS=$(curl -o /dev/null -s -w "%{http_code}" $WEBSITE_URL)

          if [ "$STATUS" != "200" ]; then
            echo "Website health check failed"
            exit 1
          fi

      # ===== TELEGRAM SUCCESS =====
      - name: Telegram Success Notification
        if: success()
        continue-on-error: true
        run: |
          STATUS=$(curl -o /dev/null -s -w "%{http_code}" $WEBSITE_URL)

          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d "text=✅ DEPLOY SUCCESS%0A%0AWorkflow: #${{ github.run_number }}%0ACommit: ${{ github.event.head_commit.message }}%0ASHA: ${{ github.sha }}%0AActor: ${{ github.actor }}%0A%0AService: ${{ env.ECS_SERVICE }}%0AWebsite: ${{ env.WEBSITE_URL }}%0AHTTP Status: ${STATUS} OK%0A%0ADeployment completed successfully"

      # ===== TELEGRAM FAILED =====
      - name: Telegram Failed Notification
        if: failure()
        continue-on-error: true
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d "text=❌ DEPLOY FAILED%0A%0AWorkflow: #${{ github.run_number }}%0ACommit: ${{ github.event.head_commit.message }}%0AActor: ${{ github.actor }}%0A%0AFailed Step:%0A${CURRENT_STEP}%0A%0ACheck Logs:%0Ahttps://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
